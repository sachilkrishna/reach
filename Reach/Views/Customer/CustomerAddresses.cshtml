
@using Reach.Models
@{
    ViewBag.Title = "CustomerAddresses";
}



@{
    List<AddressModel> lstC = ViewBag.lstC as List<AddressModel>;
}
@using (Html.BeginForm("ChangeShippingAddress", "Customer", FormMethod.Post, new { }))
{
<div class="container">
    <h2>Available Addresses</h2><br />
    <table class="table">

        @foreach (var item in lstC)
        {
            <tr>
                <th>
                    @Html.DisplayNameFor(modelItem => item.AddressId)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.Fname)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.Lname)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.Address)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.City)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.State)
                </th>
                <th>
                    @Html.DisplayNameFor(modelItem => item.Pincode)
                </th>
                <th>
                    Shipping Address
                </th>
            </tr>
            break;
        }


        @foreach (var item in lstC)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AddressId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Fname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Lname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.City)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pincode)
                </td>
                <td>
                    @*@Html.RadioButton("ShippingAddressId", new { @name= "ShippingAddressId", @value = item.AddressId })*@
                    @*@Html.RadioButtonFor(modelItem => item.ShippingAddressId, new { @value = item.AddressId })*@
                    <input type="radio" name="ShippingAddressId" required value="@item.AddressId" />
                    @Html.ActionLink("Remove", "RemoveAddress", new { Aid=item.AddressId}, new { @class = "btn btn-info" })

                </td>
            </tr>
        }

    </table>
    <p>
        @Html.ActionLink("Add New Address", "AddAddress", new { }, new { @class = "btn btn-secondary" })
        
    </p>
    <div class="text-right">
        <input type="submit" name="btnSub" class="btn btn-primary " value="Appy Changes" />
        @*@Html.ActionLink("Cancel", "OrderSummary", "Cart", new { }, new { @class = "btn btn-outline-danger" })*@
        @*<a href="@Request.UrlReferrer.AbsoluteUri">cancel and go back</a>*@
        <a href="#" class = "btn btn-outline-danger" onclick="history.go(-1)">cancel and go back</a>
    </div>
</div>


}
